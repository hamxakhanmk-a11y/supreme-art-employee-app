// Same report as /station/report, surfaced under Reports so it sits alongside
// the other registers. Re-exported rather than duplicated so the two can't drift.
export { default } from "@/app/station/report/page";

export const dynamic = "force-dynamic";
